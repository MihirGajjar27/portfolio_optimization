import React from "react";

// TODO: Add Logo file to public folder, call the file inside this component instead of the current skeleton

export default function Logo() {
	return(
		<div className={"flex w-[50px] h-[50px] bg-blue-100 text-blue-800 border-1 border-blue-800 rounded-lg items-center justify-center text-3xl"}>
			β
		</div>
	)
}